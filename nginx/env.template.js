window.__env = {
  HELIX_ENABLED: "${HELIX_ENABLED}",
  HELIX_BASE_URL: "${HELIX_BASE_URL}",
  HELIX_OAUTH_CLIENT_ID: "${HELIX_OAUTH_CLIENT_ID}",
  HELIX_OAUTH_TENANT: "${HELIX_OAUTH_TENANT}",
  HELIX_WORKSPACE_TENANT: "${HELIX_WORKSPACE_TENANT}"
};
